import { TurnosDatePipe } from './turnos-date.pipe';

describe('TurnosDatePipe', () => {
  it('create an instance', () => {
    const pipe = new TurnosDatePipe();
    expect(pipe).toBeTruthy();
  });
});
