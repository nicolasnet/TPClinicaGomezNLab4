import { EmailReduccionPipe } from './email-reduccion.pipe';

describe('EmailReduccionPipe', () => {
  it('create an instance', () => {
    const pipe = new EmailReduccionPipe();
    expect(pipe).toBeTruthy();
  });
});
