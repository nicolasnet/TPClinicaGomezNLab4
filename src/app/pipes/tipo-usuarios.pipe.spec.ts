import { TipoUsuariosPipe } from './tipo-usuarios.pipe';

describe('TipoUsuariosPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoUsuariosPipe();
    expect(pipe).toBeTruthy();
  });
});
