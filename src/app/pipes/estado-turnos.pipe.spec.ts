import { EstadoTurnosPipe } from './estado-turnos.pipe';

describe('EstadoTurnosPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoTurnosPipe();
    expect(pipe).toBeTruthy();
  });
});
