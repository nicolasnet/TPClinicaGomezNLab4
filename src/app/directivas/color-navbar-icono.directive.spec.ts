import { ColorNavbarIconoDirective } from './color-navbar-icono.directive';

describe('ColorNavbarIconoDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorNavbarIconoDirective();
    expect(directive).toBeTruthy();
  });
});
