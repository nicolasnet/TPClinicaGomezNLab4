import { ColorTipoDeUsuarioDirective } from './color-tipo-de-usuario.directive';

describe('ColorTipoDeUsuarioDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorTipoDeUsuarioDirective();
    expect(directive).toBeTruthy();
  });
});
