import { ColorNavbarFondoDirective } from './color-navbar-fondo.directive';

describe('ColorNavbarFondoDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorNavbarFondoDirective();
    expect(directive).toBeTruthy();
  });
});
